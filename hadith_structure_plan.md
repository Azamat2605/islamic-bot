# Структура данных для Хадисов и обновление CallbackData

## 1. Формат хранения хадисов

### 1.1. Структура данных
Хадисы предлагается хранить в Python-словаре (аналогично `QURAN_DATA` в `bot/data/mock_knowledge.py`) для MVP, с возможностью миграции на JSON-файлы или базу данных в будущем.

**Причины выбора Python-словаря:**
- Быстрая реализация для MVP
- Простота разработки и отладки
- Уже успешный паттерн в проекте (модуль Коран)
- Легко расширяется до внешних JSON-файлов

### 1.2. Структура одного хадиса
```python
{
    "id": "bukhari_1",           # Уникальный идентификатор (book_id + номер)
    "book_id": "bukhari",        # Идентификатор сборника
    "number": 1,                 # Номер хадиса в сборнике
    "arabic_text": "الحديث العربي...",  # Текст на арабском
    "transliteration": "Al-hadith al-arabi...",  # Транслитерация (опционально)
    "translation": {
        "kuliev": "Текст перевода Кулиева...",
        "osmanov": "Текст перевода Османова..."
    },
    "narrator": "Передатчик (иснад)",  # Цепочка передатчиков
    "chapter": "Книга веры",     # Глава/раздел
    "source": "Сахих Аль-Бухари, Книга 1, Хадис 1",  # Полный источник
    "tags": ["вера", "намаз"],   # Теги для поиска/фильтрации
    "grade": "сахих",            # Достоверность (сахих, хасан и т.д.)
    "related_ayahs": ["2:177"],  # Связанные аяты (опционально)
}
```

### 1.3. Структура сборника
```python
HADITH_DATA = {
    "books": [
        {
            "id": "bukhari",
            "name_arabic": "صحيح البخاري",
            "name_transliteration": "Sahih al-Bukhari",
            "name_translation": "Сахих Аль-Бухари",
            "total_hadiths": 7563,
            "description": "Сборник достоверных хадисов...",
            "hadiths": [...]  # Список хадисов (для MVP первые 20-30)
        },
        {
            "id": "muslim",
            "name_arabic": "صحيح مسلم",
            "name_transliteration": "Sahih Muslim",
            "name_translation": "Сахих Муслим",
            "total_hadiths": 7563,
            "description": "Второй по авторитетности сборник...",
            "hadiths": [...]
        },
        {
            "id": "nawawi",
            "name_arabic": "الأربعون النووية",
            "name_transliteration": "Al-Arba'in al-Nawawiyyah",
            "name_translation": "40 хадисов Ан-Навави",
            "total_hadiths": 42,
            "description": "Сборник из 42 важнейших хадисов...",
            "hadiths": [...]  # Все 42 хадиса
        }
    ],
    "translators": [
        {"id": "kuliev", "name": "Эльмир Кулиев"},
        {"id": "osmanov", "name": "Магомед-Нури Османов"}
    ]
}
```

### 1.4. Функции для работы с данными (аналогично `mock_knowledge.py`)
- `get_hadith_by_id(hadith_id: str) -> dict | None`
- `get_random_hadith(book_id: str | None = None) -> dict`
- `get_hadiths_by_book(book_id: str, page: int, items_per_page: int) -> list[dict]`
- `get_book_by_id(book_id: str) -> dict | None`
- `get_all_books() -> list[dict]`

## 2. Обновление CallbackData

### 2.1. Текущий класс `KnowledgeCallback`
```python
class KnowledgeCallback(CallbackData, prefix="know"):
    action: str
    section: str | None = None
    page: int | None = None
    surah_id: int | None = None
```

### 2.2. Проблема
Для модуля хадисов необходимо передавать:
1. Идентификатор сборника (`book_id`: str)
2. Идентификатор хадиса (`hadith_id`: str или int)
3. Действие (`action`): "section", "book", "hadith", "random"

Существующее поле `surah_id` предназначено только для Корана и имеет тип `int`. Для хадисов нужен строковый идентификатор.

### 2.3. Варианты решения

**Вариант A: Добавить новые поля (рекомендуется)**
```python
class KnowledgeCallback(CallbackData, prefix="know"):
    action: str
    section: str | None = None
    page: int | None = None
    surah_id: int | None = None
    book_id: str | None = None      # Новое поле для идентификатора книги
    hadith_id: str | None = None    # Новое поле для идентификатора хадиса
```

**Преимущества:**
- Минимальное изменение базового класса
- Обратная совместимость (старый код продолжает работать)
- Четкое разделение логики (surah_id для Корана, hadith_id для хадисов)
- Поддержка строковых идентификаторов для хадисов

**Недостатки:**
- Увеличивает размер callback_data (не критично)

**Вариант B: Использовать существующие поля**
- Использовать `section` для передачи book_id (например, `section="hadith_bukhari"`)
- Использовать `surah_id` для передачи номера хадиса (но тип int ограничивает)
- Использовать `page` для дополнительных параметров

**Недостатки:**
- Путаница в семантике полей
- Ограничение типов (surah_id только int)
- Сложность расширения в будущем

### 2.4. Рекомендация
**Добавить два новых поля в `KnowledgeCallback`:**
- `book_id: str | None = None`
- `hadith_id: str | None = None`

**Обоснование:**
1. **Минимальное изменение**: Добавление optional полей не ломает существующий код
2. **Чистота архитектуры**: Каждый модуль использует свои поля (Коран → surah_id, Хадисы → hadith_id)
3. **Гибкость**: Поддержка строковых идентификаторов для хадисов
4. **Расширяемость**: Легко добавить новые модули (книги, статьи) с аналогичной логикой

### 2.5. Пример использования
```python
# Вход в раздел хадисов
KnowledgeCallback(action="section", section="hadith").pack()

# Выбор сборника
KnowledgeCallback(action="hadith_book", book_id="bukhari").pack()

# Показ конкретного хадиса
KnowledgeCallback(action="hadith_view", book_id="bukhari", hadith_id="bukhari_1").pack()

# Случайный хадис
KnowledgeCallback(action="hadith_random", book_id="bukhari").pack()
```

## 3. Логика работы модуля

### 3.1. Навигация
1. **Экран 2.1 (Полка)**: Меню выбора сборника
   - Кнопки: "Сахих Аль-Бухари", "Сахих Муслим", "40 хадисов Ан-Навави"
   - При нажатии → переход к Экран 2.2 с случайным хадисом из выбранного сборника

2. **Экран 2.2 (Карточка хадиса)**:
   - Отображение одного хадиса
   - Кнопки:
     - "🎲 Случайный" (основная) → новый случайный хадис из того же сборника
     - "🔗 Источник" → показывает номер хадиса/главу
     - "📚 Сменить сборник" → возврат к Экран 2.1
     - "🔙 Назад" → возврат в меню Знаний

### 3.2. Алгоритм "Снек-контента"
1. Пользователь нажимает кнопку "📜 Хадисы" в меню Знаний
2. Система показывает Экран 2.1 (выбор сборника)
3. Пользователь выбирает сборник → система:
   - Выбирает случайный хадис из выбранного сборника
   - Немедленно показывает Экран 2.2 с этим хадисом
4. На Экран 2.2 пользователь может:
   - Нажать "🎲 Случайный" для нового случайного хадиса
   - Нажать "🔗 Источник" для деталей
   - Вернуться к выбору сборника или в меню Знаний

### 3.3. Техническая реализация
1. **Файл данных**: `bot/data/hadith_data.py` (аналогично `mock_knowledge.py`)
2. **Клавиатуры**: `bot/keyboards/inline/knowledge/hadith_kb.py`
3. **Обработчики**: `bot/handlers/sections/knowledge/hadith/`
   - `catalog.py` → Экран 2.1 (выбор сборника)
   - `reading.py` → Экран 2.2 (чтение хадиса)
4. **Обновление `KnowledgeCallback`**: Добавить поля `book_id` и `hadith_id`

## 4. План реализации

### Этап 1: Подготовка данных
- [ ] Создать структуру данных для хадисов
- [ ] Добавить 10-20 хадисов для каждого сборника (для MVP)
- [ ] Реализовать функции доступа к данным

### Этап 2: Обновление CallbackData
- [ ] Добавить поля `book_id` и `hadith_id` в `KnowledgeCallback`
- [ ] Обновить все существующие вызовы (если нужно)
- [ ] Протестировать обратную совместимость

### Этап 3: Создание клавиатур
- [ ] Создать `hadith_kb.py` с клавиатурами:
  - `get_hadith_books_kb()` → Экран 2.1
  - `get_hadith_reading_kb()` → Экран 2.2
- [ ] Реализовать логику случайного выбора

### Этап 4: Создание обработчиков
- [ ] Создать обработчик входа в раздел хадисов
- [ ] Создать обработчик выбора сборника
- [ ] Создать обработчик показа хадиса
- [ ] Создать обработчик случайного хадиса

### Этап 5: Интеграция
- [ ] Подключить роутеры в диспетчер
- [ ] Протестировать весь flow
- [ ] Добавить локализацию (тексты на русском/арабском)

### Этап 6: Оптимизация
- [ ] Добавить кэширование случайных хадисов
- [ ] Реализовать пагинацию (если нужно)
- [ ] Добавить поиск по тегам (в будущем)

## 5. Заключение

Предложенная структура обеспечивает:
1. **Минимальные изменения** в существующем коде
2. **Чистую архитектуру** с разделением ответственности
3. **Гибкость** для будущего расширения
4. **Производительность** за счет простых структур данных
5. **Удобство использования** через "снек-контент" подход

Рекомендуется начать с Этапа 1 и 2, чтобы утвердить структуру данных и обновление CallbackData перед реализацией интерфейса.
