# Техническая спецификация: Миграция главного меню на inline-интерфейс
# Technical Specification: Main Menu Migration to Inline-Only Interface

## 1. Обзор / Overview

**Цель:** Полностью перевести бота с использования Reply-клавиатур (кнопки внизу экрана) на Inline-интерфейс (кнопки, прикреплённые к сообщениям) для достижения "чистого чата".

**Проблема:** Текущая реализация использует смешанный подход:
- `/start` → ReplyKeyboardMarkup (нижняя клавиатура)
- `/menu` → InlineKeyboardMarkup (inline-кнопки)
- Подмодули частично используют inline, но навигация часто создаёт новые сообщения

**Ожидаемый результат:** Бот работает как приложение — редактирует существующие сообщения вместо отправки новых, оставляя минимум истории в чате.

## 2. Стратегия миграции / Migration Strategy

### 2.1 Этапы реализации

**Этап 1: Базовый переход (критический)**
1. Обновить `/start` хендлер в `bot/handlers/start.py`:
   - Заменить `get_main_menu()` (Reply) на `main_menu_keyboard()` (Inline)
   - Использовать `message.answer()` с inline-клавиатурой

2. Удалить ReplyKeyboardMarkup:
   - Убрать `ReplyKeyboardRemove` (не требуется, так как клавиатура больше не показывается)
   - Удалить файл `bot/keyboards/reply/__init__.py` после полного перехода

**Этап 2: Унификация навигации**
1. Стандартизировать callback_data паттерны:
   - Главное меню: `"main_menu"`
   - Назад: `"back_to_menu"`
   - Секции: `"{section}_main"` (например, `"education_main"`)

2. Обновить все существующие inline-клавиатуры:
   - Заменить устаревшие кнопки "Назад" на единый стандарт
   - Проверить все файлы в `bot/keyboards/inline/`

**Этап 3: Реализация редактирования сообщений**
1. Модифицировать хендлеры для использования `edit_message_text`:
   - При переходе между секциями → редактировать текущее сообщение
   - При возврате в главное меню → редактировать сообщение

2. Обработка исключений:
   - Если сообщение слишком старое для редактирования → отправлять новое
   - Если сообщение с фото → использовать `edit_message_caption` или отправлять новое

## 3. Новая структура клавиатур / New Keyboard Structure

### 3.1 Главное меню (уже реализовано в `bot/keyboards/main_menu.py`)

```python
def main_menu_keyboard() -> InlineKeyboardMarkup:
    """
    7 кнопок в 4 рядах:
    1. Исламский помощник
    2. Профиль/Настройки | Знания
    3. Обучение | Расписание намазов
    4. Халяль места | Календарь событий
    """
```

### 3.2 Стандартная кнопка "Назад"

```python
def back_to_menu_keyboard() -> InlineKeyboardMarkup:
    """Единая кнопка возврата в главное меню."""
    return InlineKeyboardBuilder().button(
        text=_("🏠 В главное меню"),
        callback_data="main_menu"
    ).as_markup()
```

### 3.3 Схема callback_data

```
Главное меню:
  - "islamic_assistant"    → Исламский помощник
  - "profile_settings"     → Профиль и настройки
  - "knowledge"           → Знания (Коран, хадисы)
  - "education"           → Обучение
  - "prayer_schedule"     → Расписание намазов
  - "halal_places"        → Халяль места
  - "events_calendar"     → Календарь событий

Общие действия:
  - "main_menu"           → Возврат в главное меню
  - "back"               → Шаг назад (внутри секции)
```

## 4. Навигационный поток / Navigation Flow

### 4.1 Стартовый сценарий

```
Пользователь: /start
Бот: [Отправляет сообщение с inline-клавиатурой главного меню]
     └─ Сообщение 1: "Здравствуйте! Это ваш бот-помощник..."
        └─ Клавиатура: [7 inline-кнопок]
```

### 4.2 Переход в секцию

```
Пользователь: Нажимает "Обучение" (callback_data="education")
Бот: [Редактирует Сообщение 1]
     └─ Новый текст: "Раздел: Обучение. Выберите категорию..."
        └─ Новая клавиатура: [Кнопки раздела обучения + "🏠 В главное меню"]
```

### 4.3 Возврат в главное меню

```
Пользователь: Нажимает "🏠 В главное меню" (callback_data="main_menu")
Бот: [Редактирует текущее сообщение]
     └─ Текст возвращается к: "Главное меню"
        └─ Клавиатура возвращается к главному меню (7 кнопок)
```

### 4.4 Обработка пользовательского ввода (особый случай)

**Проблема:** Некоторые функции требуют текстового ввода (AI-ассистент, поиск).

**Решение:**
1. При активации текстового режима:
   - Сохранять message_id текущего меню
   - Отправлять инструкцию: "Введите ваш вопрос..."

2. После обработки ввода:
   - Удалять сообщение пользователя (если возможно)
   - Редактировать оригинальное меню для отображения результата
   - ИЛИ отправлять новый результат с кнопкой "Назад к меню"

## 5. Техническая реализация / Technical Implementation

### 5.1 Обновление хендлера /start

```python
# Текущая реализация (bot/handlers/start.py):
await message.answer(welcome_text, reply_markup=get_main_menu())

# Новая реализация:
from bot.keyboards.main_menu import main_menu_keyboard
await message.answer(welcome_text, reply_markup=main_menu_keyboard())
```

### 5.2 Шаблон хендлера секции

```python
@router.callback_query(F.data == "education")
async def education_handler(callback: CallbackQuery) -> None:
    """Обработка перехода в раздел Обучение."""
    try:
        # Редактируем текущее сообщение
        await callback.message.edit_text(
            _("Раздел: Обучение. Выберите категорию..."),
            reply_markup=education_main_keyboard()  # Клавиатура раздела
        )
    except Exception:
        # Если редактирование невозможно, отправляем новое сообщение
        await callback.message.answer(
            _("Раздел: Обучение. Выберите категорию..."),
            reply_markup=education_main_keyboard()
        )
```

### 5.3 Глобальный обработчик возврата

```python
@router.callback_query(F.data == "main_menu")
async def main_menu_handler(callback: CallbackQuery) -> None:
    """Обработчик возврата в главное меню."""
    try:
        await callback.message.edit_text(
            _("Главное меню"),
            reply_markup=main_menu_keyboard()
        )
    except Exception:
        await callback.message.answer(
            _("Главное меню"),
            reply_markup=main_menu_keyboard()
        )
```

## 6. Оценка рисков / Risk Assessment

### 6.1 Критические риски

| Риск | Вероятность | Влияние | Миттигация |
|------|------------|---------|------------|
| Ошибка редактирования старых сообщений | Высокая | Среднее | Использовать try/except, fallback на отправку нового сообщения |
| Потеря контекста при множественных сообщениях | Средняя | Высокое | Хранить message_id текущего меню в состоянии пользователя |
| Конфликт с фото/видео сообщениями | Средняя | Среднее | Проверять тип сообщения, использовать edit_message_caption для фото |
| Увеличение нагрузки на сервер | Низкая | Низкое | Кэширование клавиатур, оптимизация запросов |

### 6.2 Совместимость

1. **Обратная совместимость:**
   - Пользователи с старыми сообщениями Reply-клавиатур увидят их до следующего взаимодействия
   - Новые пользователи сразу получат inline-интерфейс

2. **Интеграция с существующими модулями:**
   - Модули, уже использующие inline (Profile, AI Assistant), требуют минимальных изменений
   - Модули с Reply-клавиатурами (если есть) требуют полного перевода на inline

### 6.3 Тестирование

**Обязательные сценарии тестирования:**
1. `/start` → показывает inline-меню (без Reply-клавиатуры)
2. Нажатие любой кнопки → редактирование сообщения
3. Возврат в главное меню → корректное редактирование
4. Текстовый ввод в AI-ассистенте → обработка и возврат к меню
5. Обработка ошибок при редактировании старых сообщений

## 7. План внедрения / Implementation Plan

### Неделя 1: Подготовка
- [ ] Создание и утверждение спецификации
- [ ] Резервное копирование текущей реализации
- [ ] Обновление `list_files.txt`

### Неделя 2: Базовая реализация
- [ ] Обновление `/start` хендлера
- [ ] Реализация глобального обработчика `main_menu`
- [ ] Обновление 2-3 ключевых модулей (Профиль, Обучение)

### Неделя 3: Полная миграция
- [ ] Перевод всех оставшихся модулей на inline
- [ ] Удаление Reply-клавиатур из кодовой базы
- [ ] Тестирование всех пользовательских сценариев

### Неделя 4: Оптимизация и мониторинг
- [ ] Добавление метрик использования
- [ ] Оптимизация производительности
- [ ] Сбор обратной связи от пользователей

## 8. Заключение / Conclusion

Миграция на inline-интерфейс значительно улучшит пользовательский опыт, создав более "чистый" и интуитивный интерфейс. Реализация требует тщательного планирования из-за необходимости обработки ошибок редактирования сообщений, но предоставляет долгосрочные преимущества в виде единой архитектуры навигации.

**Дата создания документа:** 16.12.2025  
**Статус:** Черновик для обсуждения  
**Автор:** Архитектор UX/UI & Senior Python Developer
