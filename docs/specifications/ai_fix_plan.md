# Техническое задание: Исправление ошибок UI/UX модуля AI Assistant

## 1. Анализ ошибок

### 1.1 Проблема: Краш при переходе в режим "Общение"
**Ошибка:** `TelegramBadRequest: Bad Request: there is no caption in the message to edit`
**Файл:** `bot/handlers/sections/ai_assistant.py`, строка 75, функция `start_chat_mode`
**Причина:** 
- Функция `start_chat_mode` вызывает `callback.message.edit_caption()`, предполагая, что исходное сообщение является медиа-сообщением с caption.
- Однако сообщение могло быть отправлено как текстовое (например, через `on_ai_assistant_entry` используется `edit_text`, а `ai_assistant_entry` отправляет фото с caption).
- В зависимости от точки входа (через главное меню или через кнопку "🤖 Исламский помощник") тип сообщения различается:
  - `on_ai_assistant_entry` → текстовое сообщение (edit_text)
  - `ai_assistant_entry` → фото с caption (edit_caption)
- Функция `start_chat_mode` не проверяет тип сообщения и всегда пытается редактировать caption.

**Код проблемы:**
```python
# В start_chat_mode:
await callback.message.edit_caption(
    caption="💬 *Режим общения*...",
    parse_mode=ParseMode.MARKDOWN
)
```

### 1.2 Проблема: Навигация в режиме "Создание изображений"
**Ошибка 1:** Отправляется новое сообщение вместо редактирования текущего
- В функции `image_generation_stub` используется `callback.message.answer_photo()`, что создает новое сообщение.
- Старое меню (с кнопками "💬 ОБЩЕНИЕ", "🎨 Создание изображений", "🏠 В главное меню") остается в истории чата.

**Ошибка 2:** Отсутствует кнопка "Назад" для возврата к меню ИИ-помощника
- В клавиатуре `get_ai_menu_kb()` есть только кнопка "🏠 В главное меню", но нет кнопки для возврата внутри модуля.
- После перехода в режим генерации изображений пользователь не может вернуться в главное меню ИИ-помощника.

**Код проблемы:**
```python
# В image_generation_stub:
await callback.message.answer_photo(
    photo=TEASER_IMAGE_URL,
    caption=caption,
    parse_mode=ParseMode.MARKDOWN
)
# Нет reply_markup с кнопкой "Назад"
```

## 2. Предлагаемое решение

### 2.1 Универсальное редактирование сообщений
**Решение 1: Проверка типа сообщения**
- Создать вспомогательную функцию `safe_edit_message`, которая:
  - Проверяет `message.content_type`
  - Для `ContentType.TEXT` использует `edit_text`
  - Для `ContentType.PHOTO` использует `edit_caption`
  - Для других типов обрабатывает соответствующим образом

**Альтернативное решение:** Использовать `InputMedia` для конвертации сообщения
- Создать `InputMediaPhoto` с новым caption для медиа-сообщений
- Использовать `edit_media` для замены контента

**Рекомендуемый подход:** Универсальный метод с проверкой
```python
async def safe_edit_message(
    message: Union[Message, CallbackQuery],
    text: str,
    reply_markup: Optional[InlineKeyboardMarkup] = None,
    parse_mode: Optional[str] = None
):
    """Безопасное редактирование сообщения с учетом его типа."""
    if isinstance(message, CallbackQuery):
        message = message.message
    
    if message.content_type == ContentType.TEXT:
        return await message.edit_text(
            text=text,
            reply_markup=reply_markup,
            parse_mode=parse_mode
        )
    elif message.content_type == ContentType.PHOTO:
        return await message.edit_caption(
            caption=text,
            reply_markup=reply_markup,
            parse_mode=parse_mode
        )
    # Другие типы сообщений...
```

### 2.2 Исправление навигации
**Решение для режима генерации изображений:**
1. Заменить `answer_photo` на редактирование текущего сообщения
2. Добавить кнопку "⬅️ Назад" в клавиатуру
3. Использовать `edit_message_media` для замены фото (если нужно)

**Обновленная клавиатура для режима генерации:**
```python
def get_ai_image_mode_kb() -> InlineKeyboardMarkup:
    """Клавиатура для режима генерации изображений."""
    builder = InlineKeyboardBuilder()
    builder.row(
        InlineKeyboardButton(text="⬅️ Назад", callback_data="ai_assistant_back"),
        InlineKeyboardButton(text="🏠 В главное меню", callback_data="main_menu")
    )
    return builder.as_markup()
```

### 2.3 Улучшение FSM и состояний
**Проблема:** При переходе между режимами состояние не сохраняется корректно
**Решение:** Добавить состояние для каждого режима работы ИИ-помощника:
- `AIAssistantState.main_menu` - главное меню помощника
- `AIAssistantState.chat_mode` - режим общения
- `AIAssistantState.image_mode` - режим генерации изображений

## 3. План работ

### 3.1 Файлы для изменения

**1. `bot/handlers/sections/ai_assistant.py`**
- [ ] Добавить импорт `ContentType` из `aiogram.enums`
- [ ] Создать вспомогательную функцию `safe_edit_message`
- [ ] Исправить `start_chat_mode` для использования безопасного редактирования
- [ ] Переписать `image_generation_stub` для редактирования текущего сообщения
- [ ] Добавить обработчик для кнопки "Назад" (`ai_assistant_back`)

**2. `bot/keyboards/inline/ai_assistant.py`**
- [ ] Добавить функцию `get_ai_image_mode_kb()` с кнопкой "Назад"
- [ ] Обновить `get_ai_menu_kb()` - рассмотреть добавление кнопки "Назад" (опционально)

**3. `bot/states/ai_assistant.py`**
- [ ] Добавить новые состояния для навигации:
  ```python
  class AIAssistantState(StatesGroup):
      main_menu = State()      # Главное меню ИИ-помощника
      chat_mode = State()      # Режим общения
      image_mode = State()     # Режим генерации изображений
      waiting_for_question = State()  # Ожидание вопроса (оставить)
  ```

**4. Возможно, `bot/handlers/common_nav.py`**
- [ ] Добавить общий обработчик для кнопки "Назад" в контексте ИИ-помощника

### 3.2 Последовательность реализации

**Этап 1: Безопасное редактирование сообщений**
1. Реализовать `safe_edit_message` в `ai_assistant.py`
2. Обновить `start_chat_mode` для использования новой функции
3. Протестировать переход в режим общения из разных точек входа

**Этап 2: Исправление режима генерации изображений**
1. Создать `get_ai_image_mode_kb` с кнопкой "Назад"
2. Переписать `image_generation_stub` для использования `safe_edit_message`
3. Добавить обработчик для callback "ai_assistant_back"

**Этап 3: Улучшение навигации через FSM**
1. Обновить состояния в `ai_assistant.py`
2. Установить соответствующие состояния при переходах
3. Обеспечить корректный возврат по кнопке "Назад"

**Этап 4: Тестирование**
1. Протестировать все сценарии навигации:
   - Вход через главное меню
   - Вход через кнопку "🤖 Исламский помощник"
   - Переход между режимами общения и генерации
   - Возврат назад и в главное меню

### 3.3 Оценка сложности

**Низкая сложность:**
- Создание вспомогательной функции `safe_edit_message`
- Добавление новой клавиатуры `get_ai_image_mode_kb`

**Средняя сложность:**
- Обновление обработчиков для использования безопасного редактирования
- Добавление обработчика для кнопки "Назад"

**Высокая сложность:**
- Рефакторинг FSM для поддержки полноценной навигации
- Обработка edge cases (разные типы сообщений, ошибки API)

## 4. Ожидаемый результат

После реализации исправлений:

1. **Устранен краш** при переходе в режим общения из любой точки входа
2. **Улучшена навигация** в режиме генерации изображений:
   - Редактируется текущее сообщение вместо создания нового
   - Появилась кнопка "Назад" для возврата в меню ИИ-помощника
3. **Согласованность интерфейса** - единый подход к редактированию сообщений
4. **Расширяемость** - добавлена основа для будущих режимов работы ИИ-помощника

## 5. Резервные решения

**Если использование `InputMedia` окажется сложным:**
- Можно удалить старое сообщение и отправить новое с правильной клавиатурой
- Минус: потеря history и возможные проблемы с FSM

**Если FSM рефакторинг затруднен:**
- Использовать простые callback данные для навигации
- Хранить состояние в данных callback (например, `ai_mode:chat`, `ai_mode:image`)

---

**Дата создания:** 16.12.2025  
**Автор:** Senior Python Architect  
**Статус:** На рассмотрении
