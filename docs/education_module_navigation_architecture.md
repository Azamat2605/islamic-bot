# Архитектура навигации образовательного модуля

## Обзор
Документ описывает архитектуру навигации образовательного модуля, реализованную в рамках MVP фазы. Архитектура обеспечивает предсказуемую, консистентную и масштабируемую навигацию с полным логированием действий пользователя.

## 1. Точки входа (Entry Points)

Пользователь может попасть в главное меню образования двумя способами:

### а) Через текстовую кнопку "Обучение" (Reply Button)
```python
@router.message(F.text == __("Обучение"))
async def education_entry(message: types.Message) -> None:
    # Показывает главное меню образования
    await message.answer(text, reply_markup=get_education_main_keyboard())
```

### б) Через callback из главного меню бота
```python
@router.callback_query(F.data == "education")
async def education_callback_entry(callback: types.CallbackQuery, session: AsyncSession) -> None:
    # Показывает главное меню образования с новой стандартизированной клавиатурой
    await callback.message.edit_text(text, reply_markup=get_education_menu_keyboard())
```

## 2. Главное меню образования (Main Menu Keyboard)

### Функция: `get_education_menu_keyboard()`
**Расположение:** `bot/keyboards/inline/education.py`

**Особенности:**
- Создает сетку 2×3 (6 кнопок) + 2 отдельные кнопки внизу
- Использует `EducationCallback` с действиями

**Структура кнопок:**
```
[Каталог курсов] [Активные курсы]
[Тесты] [Прогресс]
[Завершенные курсы] [Эфиры]
[AI-помощник]
[⬅️ Назад]
```

**Действия (actions):**
1. `action="catalog"` - Каталог курсов
2. `action="active"` - Активные курсы  
3. `action="tests"` - Тесты
4. `action="progress"` - Прогресс
5. `action="completed"` - Завершенные курсы
6. `action="streams"` - Эфиры
7. `action="assistant"` - AI-помощник
8. `action="menu_back"` - Назад (возврат в предыдущее меню)

## 3. Поток подменю (Sub-Menu Flow)

Все 7 обработчиков подменю работают по единому шаблону:

### Пример для каталога курсов:
```python
@router.callback_query(EducationCallback.filter(F.action == "catalog"))
async def edu_catalog_handler(callback: types.CallbackQuery) -> None:
    # Логирование действия пользователя
    logger.info(f"User {callback.from_user.id} selected Catalog")
    
    # Текст-заглушка (dummy content) с описанием раздела
    text = _("📚 Каталог курсов\n\nЗдесь вы найдете все доступные курсы...")
    
    # Использование edit_text для плавного обновления сообщения
    with suppress(TelegramBadRequest):
        await callback.message.edit_text(
            text,
            reply_markup=get_education_sub_menu_keyboard()  # Стандартная навигация
        )
    await callback.answer()
```

### Ключевые моменты подменю:
- **Логирование:** Каждое действие пользователя логируется с его ID
- **Заглушки:** Показывают информацию о том, что функция находится в разработке
- **Обновление сообщения:** Используется `edit_text()` вместо `answer()` для сохранения контекста
- **Стандартная навигация:** Все подменю используют `get_education_sub_menu_keyboard()`

## 4. Навигация "Назад" и "В главное меню"

### а) Возврат в главное меню образования:
```python
@router.callback_query(EducationCallback.filter(F.action == "menu_back"))
async def edu_menu_back_handler(callback: types.CallbackQuery) -> None:
    logger.info(f"User {callback.from_user.id} clicked Back to Education Menu")
    
    text = _("🎓 Раздел Обучения: Выберите категорию")
    
    with suppress(TelegramBadRequest):
        await callback.message.edit_text(
            text,
            reply_markup=get_education_menu_keyboard()  # Возврат к главному меню образования
        )
    await callback.answer()
```

**Callback:** `EducationCallback(action="menu_back")`

### б) Возврат в корневое главное меню бота:
```python
@router.callback_query(F.data == "main_menu")
async def main_menu_handler(callback: types.CallbackQuery) -> None:
    logger.info(f"User {callback.from_user.id} clicked Main Menu from Education")
    
    from bot.keyboards.main_menu import main_menu_keyboard
    
    text = _("Главное меню")
    
    with suppress(TelegramBadRequest):
        await callback.message.edit_text(
            text,
            reply_markup=main_menu_keyboard()  # Возврат к корневому главному меню
        )
    await callback.answer()
```

**Callback:** `data="main_menu"`

## 5. Стандартизированная клавиатура подменю

### Функция: `get_education_sub_menu_keyboard()`
**Расположение:** `bot/keyboards/inline/education.py`

**Структура:**
```
[⬅️ Назад] [🏠 В главное меню]
```

**Назначение кнопок:**
- **"⬅️ Назад"** → `EducationCallback(action="menu_back")` → возврат в главное меню образования
- **"🏠 В главное меню"** → `data="main_menu"` → возврат в корневое главное меню бота

## 6. Обработка ошибок и плавность переходов

### Использование suppress:
```python
with suppress(TelegramBadRequest):
    await callback.message.edit_text(...)
```

**Преимущества:**
- Обработка ошибок Telegram API
- Предотвращение краха бота при проблемах с сообщениями
- Плавные переходы между экранами

### Использование edit_text():
- Сохранение контекста сообщения
- Плавная анимация перехода
- Сохранение истории переписки

## 7. Преимущества архитектуры

### 1. Предсказуемость
Пользователь всегда знает, куда ведут кнопки навигации:
- "⬅️ Назад" → возврат в предыдущее меню
- "🏠 В главное меню" → возврат в корневое меню

### 2. Консистентность
- Все подменю используют одинаковый шаблон
- Единый стиль текстов и клавиатур
- Стандартизированные callback-данные

### 3. Масштабируемость
- Легко добавлять новые разделы
- Модульная структура обработчиков
- Переиспользуемые компоненты клавиатур

### 4. Отказоустойчивость
- Обработка ошибок Telegram API
- Логирование всех действий
- Graceful degradation при проблемах

### 5. Логируемость
- Отслеживание действий пользователя
- Аналитика использования функций
- Отладка проблем навигации

## 8. Файловая структура

```
bot/
├── keyboards/
│   └── inline/
│       └── education.py              # Функции клавиатур
└── handlers/
    └── sections/
        └── education_handlers.py     # Обработчики навигации
```

## 9. Заключение

Реализованная архитектура обеспечивает:
- **Интуитивную навигацию** для пользователей
- **Легкую поддержку** для разработчиков
- **Масштабируемость** для будущих расширений
- **Надежность** через обработку ошибок
- **Аналитику** через полное логирование

Архитектура готова к интеграции реального контента курсов, тестов и других образовательных материалов в рамках следующих этапов разработки.
