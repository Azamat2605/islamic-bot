# Обзор проекта: Islamic Telegram Bot

## 1. Общее описание

Telegram-бот для мусульман, предоставляющий функционал:
- Расписание намазов с учетом города и мазхаба
- Уведомления о времени намазов
- Исламские знания, хадисы, цитаты
- Календарь исламских событий
- Халальные места (рестораны, магазины)
- Многоязычная поддержка (русский, английский, арабский, татарский, башкирский)
- Административная панель для управления пользователями

Проект построен на современном стеке Python с использованием асинхронного программирования.

## 2. Структура проекта

```
Telegrambot_test/
├── bot/                          # Основной код бота
│   ├── __init__.py
│   ├── __main__.py              # Точка входа
│   ├── core/
│   │   ├── config.py            # Конфигурация приложения
│   │   └── loader.py            # Инициализация бота, диспетчера, i18n
│   ├── handlers/                # Обработчики сообщений
│   │   ├── __init__.py          # Роутер всех обработчиков
│   │   ├── start.py             # Команда /start
│   │   ├── menu.py              # Главное меню
│   │   ├── info.py              # Информационные команды
│   │   ├── support.py           # Поддержка
│   │   ├── admins/              # Админские команды
│   │   │   ├── admin_panel.py
│   │   │   └── test_prayer.py   # Тестовые команды для уведомлений
│   │   └── sections/            # Разделы бота
│   │       ├── prayer_schedule_handlers.py  # Расписание намазов
│   │       ├── settings_handlers.py         # Настройки
│   │       ├── profile_handlers.py          # Профиль
│   │       └── ... другие разделы
│   ├── keyboards/               # Клавиатуры
│   │   ├── inline/              # Inline-кнопки
│   │   │   ├── prayers.py       # Клавиатуры для намазов
│   │   │   ├── settings.py      # Настройки
│   │   │   └── profile.py       # Профиль
│   │   └── reply/               # Reply-клавиатуры
│   ├── middlewares/             # Мидлвари
│   │   ├── __init__.py
│   │   ├── database.py          # Сессии БД
│   │   ├── i18n.py              # Локализация
│   │   ├── auth.py              # Аутентификация
│   │   ├── throttling.py        # Ограничение запросов
│   │   └── ... другие
│   ├── services/                # Бизнес-логика
│   │   ├── prayer_service.py    # Сервис расчета времени намазов
│   │   ├── scheduler.py         # Планировщик уведомлений
│   │   ├── analytics.py         # Аналитика
│   │   └── users.py             # Работа с пользователями
│   ├── filters/                 # Фильтры
│   │   ├── admin.py             # Фильтр администраторов
│   │   └── number.py            # Фильтр чисел
│   ├── states/                  # Состояния FSM
│   │   ├── profile.py           # Состояния профиля
│   │   ├── settings.py          # Состояния настроек
│   │   └── prayer.py            # Состояния намазов
│   ├── locales/                 # Локализация
│   │   ├── ru/LC_MESSAGES/
│   │   ├── en/LC_MESSAGES/
│   │   ├── ar/LC_MESSAGES/
│   │   └── ... другие языки
│   └── utils/                   # Вспомогательные утилиты
├── database/                    # Работа с базой данных
│   ├── __init__.py
│   ├── engine.py               # Создание движка и сессий
│   ├── models.py               # SQLAlchemy модели
│   ├── crud.py                 # CRUD операции
│   └── migration.py            # Миграции
├── migrations/                  # Alembic миграции
│   ├── versions/               # Файлы миграций
│   ├── env.py
│   └── script.py.mako
├── admin/                       # Веб-админка
│   ├── app.py                  # Flask приложение
│   ├── views/                  # Представления
│   └── templates/              # Шаблоны
├── configs/                     # Конфигурации для мониторинга
│   ├── prometheus/             # Prometheus
│   └── grafana/                # Grafana
├── scripts/                     # Вспомогательные скрипты
├── logs/                       # Логи приложения
├── requirements.txt            # Зависимости Python
├── pyproject.toml             # Конфигурация проекта
├── Dockerfile                  # Docker образ
├── docker-compose.yml         # Docker Compose
└── README.md                   # Документация
```

## 3. Технологический стек

### Основные технологии:
- **Python 3.11+** - основной язык программирования
- **Aiogram 3.x** - асинхронный фреймворк для Telegram ботов
- **SQLAlchemy 2.0** - ORM для работы с базой данных
- **PostgreSQL** - основная база данных (также поддерживается SQLite для разработки)
- **Alembic** - система миграций базы данных
- **APScheduler** - планировщик задач для уведомлений
- **Pydantic** - валидация конфигурации
- **Loguru** - логирование

### Вспомогательные технологии:
- **Flask + Flask-Admin** - веб-админка
- **Redis** - кэширование (опционально)
- **Prometheus + Grafana** - мониторинг и метрики
- **Docker + Docker Compose** - контейнеризация

## 4. Архитектура бота

### 4.1. Инициализация (`bot/core/loader.py`)
- Создание экземпляров `Bot`, `Dispatcher`, `I18n`
- Настройка мидлварей в правильном порядке
- Инициализация планировщика уведомлений
- Регистрация всех обработчиков через роутеры

### 4.2. Цепочка мидлварей (порядок выполнения):
1. `PrometheusMiddleware` - сбор метрик
2. `LoggingMiddleware` - логирование запросов
3. `ThrottlingMiddleware` - ограничение запросов
4. `DatabaseMiddleware` - управление сессиями БД
5. `I18nMiddleware` - локализация
6. `AuthMiddleware` - аутентификация пользователей
7. `ChannelSubscribeMiddleware` - проверка подписки на каналы

### 4.3. Система роутеров:
- Каждый раздел бота имеет свой роутер
- Все роутеры собираются в `bot/handlers/__init__.py`
- Основные роутеры:
  - `start.router` - команда /start
  - `menu.router` - главное меню
  - `prayer_schedule_router` - расписание намазов
  - `settings_router` - настройки
  - `admin_panel` - админские команды
  - `test_prayer` - тестовые команды для уведомлений

### 4.4. Локализация (i18n):
- Используется стандартная система i18n от aiogram
- Переводы хранятся в `bot/locales/{lang}/LC_MESSAGES/messages.po`
- Поддерживаемые языки: ru, en, ar, tt, ba
- Автоматическое определение языка пользователя

## 5. База данных и модели

### 5.1. Модели данных:
```python
class User(Base):
    id: int (PK)
    telegram_id: int (unique)
    username: str (optional)
    full_name: str
    city: str (optional)
    created_at: datetime
    is_admin: bool (default=False)
    settings: relationship → Settings

class Settings(Base):
    id: int (PK)
    user_id: int (FK → User.id)
    language: str (default='ru')
    notification_on: bool (default=True)
    madhab: str (default='Hanafi')
    prayer_notifications_on: bool (default=True)
    notify_fajr: bool (default=True)
    notify_dhuhr: bool (default=True)
    notify_asr: bool (default=True)
    notify_maghrib: bool (default=True)
    notify_isha: bool (default=True)
    timezone: str (optional)
```

### 5.2. Ключевые CRUD операции:
- `ensure_user()` - гарантированное создание/обновление пользователя
- `get_user_with_settings()` - получение пользователя с настройками
- `get_user_language()` - получение языка пользователя
- `update_settings()` - обновление настроек

### 5.3. Миграции:
- Используется Alembic для управления миграциями
- Миграции хранятся в `migrations/versions/`
- Автоматическое создание миграций через скрипты

## 6. Ключевые компоненты

### 6.1. Сервис расчета времени намазов (`bot/services/prayer_service.py`)
- Интеграция с внешним API для получения времени намазов
- Поддержка различных мазхабов (Hanafi, Shafi, Maliki, Hanbali)
- Кэширование результатов для оптимизации
- Поддержка городов Башкирии и других регионов

### 6.2. Планировщик уведомлений (`bot/services/scheduler.py`)
- Автоматическая проверка времени намазов каждую минуту
- Отправка уведомлений пользователям с включенными уведомлениями
- Учет индивидуальных настроек (какие намазы уведомлять)
- Обработка ошибок (заблокированные боты, сетевые проблемы)

### 6.3. Система уведомлений:
- Формат сообщения: `🕌 Время намаза {намаз} в г. {город}!`
- Локализация названий намазов
- Учет часового пояса пользователя
- Тестовые команды для проверки (`/test_notify`)

### 6.4. Административные функции:
- Команда `/admin` - панель администратора
- Экспорт пользователей в CSV
- Статистика использования бота
- Тестовые команды для отладки

## 7. Развертывание и запуск

### 7.1. Локальная разработка:
```bash
# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env
# Редактирование .env файла

# Запуск миграций
alembic upgrade head

# Запуск бота
python -m bot
```

### 7.2. Docker развертывание:
```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot
```

### 7.3. Конфигурация:
- Основные настройки в `.env` файле
- Токен бота: `BOT_TOKEN`
- Настройки базы данных: `DB_HOST`, `DB_PORT`, `DB_NAME`, etc.
- Список администраторов: `ADMINS` (через запятую)
- Режим отладки: `DEBUG`

## 8. Мониторинг и логирование

### 8.1. Логирование:
- Используется Loguru для структурированного логирования
- Логи хранятся в `logs/` с ротацией по дням
- Уровни логирования: DEBUG, INFO, WARNING, ERROR

### 8.2. Метрики:
- Prometheus метрики через `PrometheusMiddleware`
- Доступны по адресу `/metrics`
- Графики в Grafana для мониторинга активности

### 8.3. Веб-админка:
- Flask-приложение на порту 5000
- Управление пользователями
- Просмотр статистики
- Доступ только для администраторов

## 9. Особенности реализации

### 9.1. Асинхронная архитектура:
- Все операции ввода-вывода асинхронные
- Использование async/await паттернов
- Неблокирующие запросы к API и БД

### 9.2. Обработка ошибок:
- Централизованная обработка исключений
- Грейсфул деградация при недоступности внешних сервисов
- Автоматическое восстановление после ошибок

### 9.3. Безопасность:
- Валидация входящих данных
- Защита от спама и флуда
- Безопасное хранение токенов и ключей

## 10. Дальнейшее развитие

### Планируемые улучшения:
1. **Расширение географии** - поддержка большего количества городов
2. **Интеграция с календарем** - синхронизация с Google Calendar, Apple Calendar
3. **Голосовые уведомления** - аудио-напоминания о намазах
4. **Социальные функции** - общие молитвы, напоминания друзьям
5. **Мобильное приложение** - нативное приложение на основе того же бэкенда

### Технические улучшения:
1. **Кэширование Redis** - для повышения производительности
2. **Кластеризация** - горизонтальное масштабирование
3. **Тестирование** - покрытие unit и интеграционными тестами
4. **CI/CD** - автоматизация развертывания

---

*Последнее обновление: 10.12.2025*
*Версия проекта: 2.0.0*
