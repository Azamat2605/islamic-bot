# Техническая спецификация: Модуль образования (Ilm) для Telegram бота

## 1. Обзор проекта

### 1.1 Контекст
- **Тема:** Исламское образование (академическое, уважительное, чистый UI)
- **Технологический стек:** Python 3.10+, Aiogram 3.x, SQLAlchemy (Async), PostgreSQL
- **Философия UI:** "Single Window" (Dashboard). Минимизация отправки новых сообщений. Использование `edit_text` для навигации между Menu → Course → Lesson → Test.

### 1.2 Текущее состояние
Проект уже имеет базовую структуру образовательного модуля:
- Модели базы данных для курсов, модулей, тестов и прогресса
- Сервисный слой `EducationService` с базовыми методами
- Обработчики и клавиатуры для образовательного модуля
- Интеграция с главным меню бота

### 1.3 Цель
Создание полноценной системы обучения с 7 экранами, включая дашборд, каталог курсов, прохождение уроков, тестирование, эфиры, завершенные курсы и AI-помощника.

## 2. ER-диаграмма и схема базы данных

### 2.1 Существующие таблицы

#### Users (Пользователи)
```sql
id: Integer (PK)
telegram_id: BigInteger (Unique)
username: String (Nullable)
full_name: String
gender: String (Nullable)
city: String (Nullable)
streak_days: Integer
education_level: String(50) (Nullable)
total_courses_completed: Integer
total_tests_passed: Integer
learning_streak_days: Integer
last_learning_activity: DateTime (Nullable)
created_at: DateTime
```

#### Settings (Настройки)
```sql
id: Integer (PK)
user_id: BigInteger (FK → Users, Unique)
language: String
notification_on: Boolean
-- Поля для уведомлений об обучении
notify_course_reminders: Boolean
notify_test_results: Boolean
notify_new_courses: Boolean
daily_learning_goal_minutes: Integer
preferred_learning_time: String(50) (Nullable)
auto_continue_courses: Boolean
```

#### Courses (Курсы)
```sql
id: Integer (PK)
title: String(200)
description: Text
short_description: String(500) (Nullable)
level: Enum(beginner, intermediate, advanced)
status: Enum(draft, published, archived)
cover_image_url: String(500) (Nullable)
estimated_hours: Integer
order_index: Integer
created_at: DateTime
updated_at: DateTime
```

#### CourseModules (Модули курса)
```sql
id: Integer (PK)
course_id: Integer (FK → Courses)
title: String(200)
description: Text (Nullable)
content_type: String(50) (video, audio, text)
content_url: String(500) (Nullable)
content_text: Text (Nullable)
duration_minutes: Integer
order_index: Integer
is_free: Boolean
created_at: DateTime
updated_at: DateTime
```

#### Tests (Тесты)
```sql
id: Integer (PK)
title: String(200)
description: Text (Nullable)
course_id: Integer (FK → Courses, Nullable)
difficulty: String(50)
time_limit_minutes: Integer (Nullable)
passing_score: Integer
max_attempts: Integer (Nullable)
is_active: Boolean
created_at: DateTime
```

#### TestQuestions (Вопросы теста)
```sql
id: Integer (PK)
test_id: Integer (FK → Tests)
question_text: Text
question_type: String(50) (single_choice, multiple_choice)
order_index: Integer
points: Integer
```

#### TestOptions (Варианты ответов)
```sql
id: Integer (PK)
question_id: Integer (FK → TestQuestions)
option_text: Text
is_correct: Boolean
order_index: Integer
explanation: Text (Nullable)
```

#### UserCourseProgress (Прогресс по курсу)
```sql
id: Integer (PK)
user_id: BigInteger (FK → Users)
course_id: Integer (FK → Courses)
status: String(50)
progress_percentage: Float
completed_modules_count: Integer
total_modules_count: Integer
started_at: DateTime
completed_at: DateTime (Nullable)
last_accessed_at: DateTime
UNIQUE(user_id, course_id)
```

#### UserModuleProgress (Прогресс по модулю)
```sql
id: Integer (PK)
user_id: BigInteger (FK → Users)
module_id: Integer (FK → CourseModules)
course_progress_id: Integer (FK → UserCourseProgress, Nullable)
status: String(50)
completed_at: DateTime (Nullable)
time_spent_minutes: Integer
notes: Text (Nullable)
UNIQUE(user_id, module_id)
```

#### UserTestResults (Результаты тестов)
```sql
id: Integer (PK)
user_id: BigInteger (FK → Users)
test_id: Integer (FK → Tests)
score: Float
correct_answers: Integer
total_questions: Integer
time_spent_seconds: Integer
attempt_number: Integer
passed: Boolean
started_at: DateTime
completed_at: DateTime (Nullable)
UNIQUE(user_id, test_id, attempt_number)
```

#### UserTestAnswers (Ответы пользователя)
```sql
id: Integer (PK)
test_result_id: Integer (FK → UserTestResults)
question_id: Integer (FK → TestQuestions)
selected_option_ids: Text (Nullable)
answer_text: Text (Nullable)
is_correct: Boolean
points_earned: Float
```

### 2.2 Новые таблицы (требуются)

#### Streams (Эфиры)
```sql
id: Integer (PK)
title: String(200)
description: Text (Nullable)
speaker: String(100) (Nullable)
scheduled_time: DateTime
duration_minutes: Integer
stream_url: String(500) (Nullable)
recording_url: String(500) (Nullable)
is_upcoming: Boolean
max_participants: Integer (Nullable)
created_at: DateTime
```

#### StreamReminders (Напоминания об эфирах)
```sql
id: Integer (PK)
user_id: BigInteger (FK → Users)
stream_id: Integer (FK → Streams)
reminded_at: DateTime (Nullable)
created_at: DateTime
UNIQUE(user_id, stream_id)
```

#### Certificates (Сертификаты)
```sql
id: Integer (PK)
user_id: BigInteger (FK → Users)
course_id: Integer (FK → Courses)
certificate_url: String(500)
issued_at: DateTime
score: Float
UNIQUE(user_id, course_id)
```

### 2.3 Связи между таблицами
```
Users (1) → (∞) UserCourseProgress
Users (1) → (∞) UserModuleProgress  
Users (1) → (∞) UserTestResults
Users (1) → (∞) StreamReminders
Users (1) → (∞) Certificates

Courses (1) → (∞) CourseModules
Courses (1) → (∞) UserCourseProgress
Courses (1) → (∞) Tests
Courses (1) → (∞) Certificates

CourseModules (1) → (∞) UserModuleProgress

Tests (1) → (∞) TestQuestions
Tests (1) → (∞) UserTestResults

TestQuestions (1) → (∞) TestOptions
TestQuestions (1) → (∞) UserTestAnswers

Streams (1) → (∞) StreamReminders
```

## 3. Структура файлов проекта

### 3.1 Существующая структура
```
bot/
├── handlers/
│   ├── sections/
│   │   ├── education_handlers.py    # Основные обработчики обучения
│   │   ├── test_handlers.py         # Обработчики тестов
│   │   └── __init__.py
│   └── __init__.py
├── keyboards/
│   ├── inline/
│   │   ├── education.py             # Клавиатуры для обучения
│   │   └── test.py                  # Клавиатуры для тестов
│   └── __init__.py
├── services/
│   ├── education_service.py         # Сервис для бизнес-логики обучения
│   └── __init__.py
├── states/
│   ├── test.py                      # Состояния FSM для тестов
│   └── __init__.py
└── __init__.py
```

### 3.2 Новая структура (дополнения)
```
bot/
├── handlers/
│   ├── sections/
│   │   ├── education_handlers.py    # Расширенный: все 7 экранов
│   │   ├── test_handlers.py         # Расширенный: два режима тестирования
│   │   ├── stream_handlers.py       # Новый: эфиры и записи
│   │   ├── ai_assistant_handlers.py # Новый: AI-помощник
│   │   └── __init__.py
│   └── __init__.py
├── keyboards/
│   ├── inline/
│   │   ├── education.py             # Расширенный: клавиатуры для 7 экранов
│   │   ├── test.py                  # Расширенный: клавиатуры тестов
│   │   ├── stream.py                # Новый: клавиатуры эфиров
│   │   ├── ai_assistant.py          # Новый: клавиатуры AI-помощника
│   │   └── __init__.py
│   └── __init__.py
├── services/
│   ├── education_service.py         # Расширенный: расчет прогресса, управление курсами
│   ├── stream_service.py            # Новый: управление эфирами и напоминаниями
│   ├── ai_service.py                # Новый: AI-помощник с ограниченным промптом
│   ├── certificate_service.py       # Новый: генерация сертификатов
│   └── __init__.py
├── states/
│   ├── education.py                 # Новый: состояния FSM для обучения
│   ├── test.py                      # Расширенный: состояния FSM для тестов
│   ├── stream.py                    # Новый: состояния FSM для эфиров
│   ├── ai_assistant.py              # Новый: состояния FSM для AI-помощника
│   └── __init__.py
└── __init__.py

database/
├── models.py                        # Расширенный: новые модели Stream, StreamReminder, Certificate
└── __init__.py

migrations/
├── versions/
│   └── [дата]_add_stream_tables.py  # Новая миграция для таблиц эфиров
└── __init__.py
```

## 4. State Machine (FSM)

### 4.1 CourseLearningState (Прохождение курса)
```python
class CourseLearningState(StatesGroup):
    selecting_course = State()        # Выбор курса
    viewing_module = State()          # Просмотр модуля
    watching_video = State()          # Просмотр видео
    listening_audio = State()         # Прослушивание аудио
    reading_telegraph = State()       # Чтение Telegraph статьи
    answering_quiz = State()          # Ответ на тест после урока
    taking_notes = State()            # Создание заметок
    module_completed = State()        # Модуль завершен
```

### 4.2 TestTakingState (Прохождение теста)
```python
class TestTakingState(StatesGroup):
    selecting_test = State()          # Выбор теста
    answering_question = State()      # Ответ на вопрос
    question_explanation = State()    # Объяснение ответа
    reviewing_results = State()       # Просмотр результатов
    test_completed = State()          # Тест завершен
```

### 4.3 StreamState (Работа с эфирами)
```python
class StreamState(StatesGroup):
    browsing_streams = State()        # Просмотр списка эфиров
    viewing_stream = State()          # Просмотр деталей эфира
    setting_reminder = State()        # Установка напоминания
    watching_recording = State()      # Просмотр записи
```

### 4.4 AIAssistantState (AI-помощник)
```python
class AIAssistantState(StatesGroup):
    waiting_for_query = State()       # Ожидание запроса
    processing_query = State()        # Обработка запроса
    showing_answer = State()          # Показ ответа
```

## 5. Карта навигации

### 5.1 Основной поток
```
Главное меню бота
    ↓
Кнопка "Обучение" (Education)
    ↓
Экран 1: Главный дашборд обучения
    ├──▶ "Продолжить обучение" → Последний незавершенный урок
    ├──▶ "Каталог курсов" → Экран 2: Каталог курсов
    ├──▶ "Завершенные курсы" → Экран 6: Завершенные курсы
    ├──▶ "Раздел Тесты" → Экран 4: Тестирование
    ├──▶ "Эфиры и Записи" → Экран 5: Эфиры
    └──▶ "Исламский Помощник" → Экран 7: AI-помощник
```

### 5.2 Детальная навигация

#### Экран 2: Каталог курсов
```
Категории курсов (Акыда, Фикх, Коран, История)
    ↓
Список курсов в категории
    ↓
Страница курса
    ├──▶ "Начать курс" → Экран 3: Первый урок
    ├──▶ "Уроки" → Список уроков с статусами (✅, 🔄, ⏳)
    └──▶ "Назад" → Категории
```

#### Экран 3: Прохождение урока (LMS Core)
```
Заголовок урока + прогресс "Шаг X из Y"
    ├──▶ "▶ Смотреть видео" → Отправка видео (новое сообщение)
    ├──▶ "🎧 Слушать аудио" → Отправка аудио (новое сообщение)
    ├──▶ "📝 Читать подробно" → Ссылка на Telegraph
    └──▶ "✅ Я изучил / Далее" → Проверка/Тест → Следующий урок
```

#### Экран 4: Тестирование
```
Режим А: Тест после урока (обязательный)
    ↓
1-3 вопроса для проверки понимания
    ├── Неправильно → Объяснение → "Попробовать снова"
    └── Правильно → Урок завершен → Следующий урок

Режим Б: Раздел "Тесты" (факультатив)
    ↓
Выбор теста → Прохождение → Результаты
```

#### Экран 5: Эфиры и записи
```
Предстоящие эфиры (с таймером)
    ├──▶ "🔔 Напомнить" → Установка напоминания
    └──▶ "Войти" → Ссылка на трансляцию

Архив записей (пагинация)
    └──▶ Запись → Просмотр
```

#### Экран 6: Завершенные курсы
```
Список завершенных курсов с медалями (🥇, 🥈, 🥉)
    ├──▶ "📜 Скачать сертификат" → Генерация PDF/изображения
    └──▶ "🔄 Пройти заново" → Сброс прогресса
```

#### Экран 7: AI-помощник
```
Приветствие + ограничения (без фетв)
    ↓
Поле для ввода вопроса
    ↓
Ответ AI (только факты, история, определения)
```

## 6. Детальные пользовательские сценарии

### 6.1 Экран 1: Главный дашборд
**Элементы:**
- Динамический текстовый прогресс-бар (пример: "🟩🟩⬜️⬜️ 50%")
- Текущий статус (пример: "Студент", "Ищущий знания")
- Кнопка "Продолжить" → ведет к последнему незавершенному уроку
- Навигация: Каталог, Завершенные курсы, Тесты, Эфиры, AI

**Формула прогресса:**
```
Прогресс = (Количество пройденных уроков пользователя / Общее количество уроков в базе) × 100
```

### 6.2 Экран 2: Каталог курсов
**Логика отображения уроков:**
- ✅ Завершенные уроки
- 🔄 Текущий урок (активный)
- ⏳ Будущие уроки (заблокированы, опционально)

**Фильтрация:** По категориям (Акыда, Фикх, Коран, История)

### 6.3 Экран 3: Просмотр урока
**Медиа-контент:**
- Кнопки [▶ Watch Video] или [🎧 Listen Audio]
- *Ограничение:* Видео/аудио отправляются НОВЫМ сообщением, но меню остается для управления потоком

**Контент:**
- Краткий "Конспект" в тексте + кнопка для полной статьи Telegraph
- Кнопка [✅ Mark as Studied]
  - *Логика:* Нажатие запускает проверку/тест. Нельзя продолжить без прохождения

### 6.4 Экран 4: Тестирование и викторины
**Пост-уроковый тест:** 1-3 вопроса для проверки понимания
- Если неправильно: показать объяснение и "Попробовать снова"
- Если правильно: отметить урок как завершенный в БД → разблокировать следующий урок

**Глобальный раздел тестов:** Случайные вопросы из БД для получения XP

### 6.5 Экран 5: Эфиры (Live & Archive)
**Предстоящие:** Показать таймер обратного отсчета + кнопка "Уведомить меня"
**Архив:** Пагинированный список прошедших эфиров со ссылками на записи

### 6.6 Экран 6: Исламский AI-помощник
**RAG-based логика (концепция)**
- *Строгое ограничение:* AI должен иметь системный промпт, чтобы НИКОГДА не давать фетвы (религиозные решения). Он предоставляет только факты, историю и определения.

## 7. Пошаговый план реализации

### Этап 1: Доработка моделей и миграций (2 дня)
1. Добавить недостающие поля в существующие модели:
   - `UserCourseProgress.is_completed: Boolean`
   - `CourseModule.has_test: Boolean`, `CourseModule.test_id: Integer`
   - `Test.module_id: Integer`
2. Создать новые модели:
   - `Stream`, `StreamReminder`, `Certificate`
3. Создать миграции Alembic
4. Применить миграции к базе данных

### Этап 2: Расширение сервисного слоя (3 дня)
1. Дополнить `EducationService` методами:
   - Расчет общего прогресса по формуле
   - Получение последней активности пользователя
   - Управление прохождением уроков
2. Создать `StreamService`:
   - Управление эфирами и напоминаниями
   - Пагинация архива записей
3. Создать `AIService`:
   - Обработка запросов с ограниченным промптом
   - Интеграция с AI API (OpenAI/Claude)
4. Создать `CertificateService`:
   - Генерация сертификатов в формате PDF/изображения

### Этап 3: Реализация FSM состояний (2 дня)
1. Создать расширенные состояния для курсов:
   - `CourseLearningState` (8 состояний)
2. Расширить состояния для тестов:
   - `TestTakingState` (5 состояний)
3. Создать состояния для эфиров:
   - `StreamState` (4 состояния)
4. Создать состояния для AI-помощника:
   - `AIAssistantState` (3 состояния)

### Этап 4: Доработка клавиатур (2 дня)
1. Создать клавиатуры для всех 7 экранов:
   - Главный дашборд
   - Каталог курсов с фильтрацией
   - Меню урока с медиа-кнопками
   - Тесты с навигацией по вопросам
   - Эфиры с таймерами
   - Завершенные курсы с медалями
   - AI-помощник с ограничениями
2. Реализовать динамические клавиатуры:
   - Статусы уроков (✅, 🔄, ⏳)
   - Прогресс-бары
   - Пагинация для каталога и архива

### Этап 5: Реализация обработчиков (4 дня)
1. Доработать существующие обработчики:
   - `education_handlers.py` для всех 7 экранов
   - `test_handlers.py` для двух режимов тестирования
2. Создать новые обработчики:
   - `stream_handlers.py` для эфиров и записей
   - `ai_assistant_handlers.py` для AI-помощника
3. Интегрировать FSM состояния
4. Реализовать обработку медиа-контента:
   - Отправка видео/аудио файлов
   - Интеграция с Telegraph API

### Этап 6: Интеграция с внешними сервисами (3 дня)
1. Интеграция с Telegraph для длинных текстов
2. Реализация отправки видео и аудио через Telegram API
3. Интеграция с AI API (OpenAI GPT/Claude)
4. Генерация сертификатов (изображения/PDF)

### Этап 7: Планировщик уведомлений (2 дня)
1. Реализация напоминаний об обучении:
   - Ежедневные цели обучения
   - Напоминания о незавершенных уроках
2. Реализация напоминаний об эфирах:
   - За 15 минут до начала
   - За 1 день до эфира
3. Интеграция с существующим планировщиком

### Этап 8: Тестирование и отладка (3 дня)
1. Модульное тестирование сервисов
2. Интеграционное тестирование обработчиков
3. Тестирование FSM состояний
4. Тестирование уведомлений
5. Исправление ошибок и оптимизация

## 8. Ключевые технические решения

### 8.1 Расчет прогресса
```python
async def calculate_overall_progress(user_id: int) -> float:
    total_lessons = await get_total_lessons_in_database()
    completed_lessons = await get_user_completed_lessons(user_id)
    return (completed_lessons / total_lessons) * 100
```

### 8.2 Навигация "Single Window"
- Использование `edit_text` вместо `send_message` для навигации
- Сохранение контекста в FSM
- Минимизация новых сообщений

### 8.3 Ограничения AI-помощника
```python
SYSTEM_PROMPT = """
Ты исламский ассистент. Отвечай только фактами из проверенных источников.
Правила:
1. Отвечай только на вопросы, связанные с исламом, Кораном, хадисами, историей
2. Не выноси фетвы (решения о халяле/хараме)
3. Не решай личные или семейные споры
4. Если вопрос сложный или требует фикх-анализа, отправляй к ученым
5. Используй только информацию из базы знаний бота
6. Будь краток и точен

Если вопрос вне компетенции, вежливо откажись отвечать.
"""
```

### 8.4 Управление медиа-контентом
- Видео/аудио: отправка как новое сообщение с `reply_to_message_id`
- Текст: встроенный контент + ссылка на Telegraph для длинных статей
- Telegraph: автоматическая генерация статей из контента уроков

## 9. Мониторинг и аналитика

### 9.1 Метрики для отслеживания
1. **Активность пользователей:**
   - Количество активных пользователей в разделе обучения
   - Среднее время обучения в день
   - Количество завершенных курсов
   - Процент возвращающихся пользователей

2. **Эффективность контента:**
   - Самые популярные курсы
   - Процент завершения курсов
   - Средние результаты тестов
   - Время, затраченное на разные типы контента

3. **Технические метрики:**
   - Время отклика сервисов
   - Количество ошибок
   - Загрузка медиа-контента

### 9.2 Интеграция с аналитикой
- Amplitude для пользовательской аналитики
- Sentry для отслеживания ошибок
- Prometheus для технических метрик
- Grafana для визуализации

## 10. Критерии успеха

### 10.1 Функциональные критерии
- [ ] Все 7 экранов реализованы согласно требованиям
- [ ] Навигация "Single Window" работает корректно
- [ ] Расчет прогресса по формуле работает точно
- [ ] AI-помощник не дает фетвы и соблюдает ограничения
- [ ] Тестирование после уроков работает в двух режимах
- [ ] Уведомления об эфирах и обучении работают

### 10.2 Технические критерии
- [ ] Код покрыт тестами (минимум 80%)
- [ ] Обработка ошибок на всех уровнях
- [ ] Производительность: время отклика < 2 секунд
- [ ] Масштабируемость: поддержка 10k+ пользователей
- [ ] Безопасность: нет утечек данных, валидация входных данных

### 10.3 Пользовательские критерии
- [ ] Интуитивный интерфейс
- [ ] Быстрая навигация между экранами
- [ ] Понятные сообщения об ошибках
- [ ] Поддержка нескольких языков (русский, английский, арабский)

## 11. Риски и митигация

### 11.1 Технические риски
1. **Производительность при большом количестве пользователей:**
   - Митигация: кэширование прогресса, индексы в БД, асинхронные операции

2. **Интеграция с внешними API (AI, Telegraph):**
   - Митигация: таймауты, повторные попытки, фолбэки

3. **Хранение медиа-контента:**
   - Митигация: CDN для видео/аудио, компрессия, lazy loading

### 11.2 Бизнес-риски
1. **Неправильное толкование исламского контента:**
   - Митигация: проверка контента экспертами, ограничения AI

2. **Юридические требования:**
   - Митигация: консультация с юристами, соблюдение законодательства

## 12. Заключение

Данная техническая спецификация предоставляет полный план для реализации модуля образования (Ilm) в Telegram боте. Реализация включает:

1. **7 полноценных экранов** с детальной бизнес-логикой
2. **Расширенную систему прогресса** с расчетом по формуле из требований
3. **Интеграцию с медиа-контентом** (видео, аудио, Telegraph)
4. **AI-помощника** с ограниченным промптом для безопасных ответов
5. **Систему уведомлений** и напоминаний
6. **Генерацию сертификатов** для завершенных курсов
7. **Поддержку многопользовательской работы** и локализации

### 12.1 Следующие шаги
1. Утверждение спецификации заинтересованными сторонами
2. Начало реализации с Этапа 1 (Модели и миграции)
3. Регулярные демонстрации прогресса
4. Тестирование с реальными пользователями

### 12.2 Контакты
- **Технический архитектор:** Senior Full-Stack Python Developer
- **Дата создания:** 11.12.2025
- **Версия документа:** 1.0
- **Статус:** Готов к реализации

---
*Документ создан на основе анализа существующей кодовой базы и требований пользователя. Все решения учитывают текущую архитектуру проекта и лучшие практики разработки.*
