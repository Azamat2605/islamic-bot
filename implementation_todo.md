# План реализации подмодуля "Хадисы"

## ШАГ 1: Обновление CallbackData
- [ ] Добавить поля `book_id` и `hadith_id` в класс `KnowledgeCallback` (файл `bot/keyboards/inline/knowledge/main_kb.py`)

## ШАГ 2: Главное меню раздела (Полка сборников)
- [ ] Убедиться, что кнопка "📜 Хадисы" уже есть в `get_knowledge_main_keyboard()` (она уже есть)
- [ ] Создать обработчик в `bot/handlers/sections/knowledge/hadith/catalog.py`:
  - Роутер с именем `hadith_catalog`
  - Обработчик для `KnowledgeCallback.filter(action="section", section="hadith")`
  - Клавиатура со списком книг (3 кнопки)
  - CallbackData: `KnowledgeCallback(action="open_book", book_id="bukhari")` и т.д.
- [ ] Создать клавиатуру для выбора книг (можно в том же файле или отдельном `hadith_kb.py`)

## ШАГ 3: Экран просмотра Хадиса (Бесконечная лента)
- [ ] Создать обработчик для `KnowledgeCallback.filter(action="open_book")`
  - Извлекает `book_id`
  - Загружает данные книги (из `hadith_data.py`)
  - Выбирает случайный хадис
  - Формирует сообщение
  - Клавиатура: "🎲 Еще хадис" (тот же action) и "Назад" (возврат к выбору книг)
- [ ] Создать файл с данными `bot/data/hadith_data.py` с минимальными тестовыми хадисами

## ШАГ 4: Файловая структура и интеграция
- [ ] Создать директорию `bot/handlers/sections/knowledge/hadith/`
- [ ] Создать `__init__.py` в этой директории
- [ ] Зарегистрировать роутер в `bot/handlers/sections/knowledge/__init__.py`
- [ ] Протестировать flow

## Дополнительно
- [ ] Убедиться, что все callback_data создаются через `.pack()`
- [ ] Использовать правильные фильтры (именованные аргументы)
- [ ] Добавить обработку ошибок и логирование
